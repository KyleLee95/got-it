import SchedulerWizard from "./scheduler-wizard";
import SchedulerLayout from "./scheduler-layout";
const Scheduler = () => {
  return (
    <SchedulerLayout>
      <SchedulerWizard />
    </SchedulerLayout>
  );
};
export default Scheduler;
